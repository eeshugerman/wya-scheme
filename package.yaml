name: wya-scheme

dependencies:
  - base
  - parsec
  - text
  - array
  - mtl


executables:
  wya-scheme-exe:
    main: Main.hs
    source-dirs: app
    ghc-options:
      - -Wall
      - -Wno-unused-do-bind
